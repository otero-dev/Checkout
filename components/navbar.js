import React from 'react';

import { NavBar, Container } from './basic'

const Nav = (props) => {
    return (
        <NavBar>
            <Container>
                Store logo
            </Container>
        </NavBar>
    )
}

export default Nav;